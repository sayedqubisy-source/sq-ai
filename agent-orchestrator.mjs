import express from 'express';
import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
import Database from 'better-sqlite3';
import { generateText } from './ai-runtime.mjs';

const execFileAsync = promisify(execFile);
const dbPath = process.env.DB_PATH || './data/sq-ai.sqlite';
const db = new Database(dbPath); db.pragma('busy_timeout=5000');
const mediaDir = path.join(path.dirname(path.resolve(dbPath)), 'generated-media');
const videoDir = path.join(path.dirname(path.resolve(dbPath)), 'generated-videos');
fs.mkdirSync(mediaDir, { recursive: true }); fs.mkdirSync(videoDir, { recursive: true });
const sleep = ms => new Promise(r => setTimeout(r, ms));
const clean = (v, max = 12000) => String(v ?? '').trim().slice(0, max);
const hash = t => crypto.createHash('sha256').update(t).digest('hex');
function cookies(req){const out={};for(const p of String(req.headers.cookie||'').split(';')){const i=p.indexOf('=');if(i>0){try{out[p.slice(0,i).trim()]=decodeURIComponent(p.slice(i+1))}catch{}}}return out}
function currentUser(req){const c=cookies(req).sqai_session;if(c){const s=db.prepare("SELECT user_id FROM sessions WHERE token_hash=? AND expires_at>datetime('now')").get(hash(c));if(s)return db.prepare('SELECT * FROM users WHERE id=?').get(s.user_id)}const k=req.get('x-api-key');return k?db.prepare('SELECT u.* FROM users u JOIN api_keys a ON a.user_id=u.id WHERE a.key=?').get(k)||null:null}
function charge(id,ep){return db.transaction(()=>{const r=db.prepare('UPDATE users SET credits=credits-1 WHERE id=? AND credits>0').run(id);if(!r.changes)return false;db.prepare('INSERT INTO usage(user_id,endpoint,units) VALUES(?,?,1)').run(id,ep);return true})()}
function refund(id,ep){try{db.transaction(()=>{db.prepare('UPDATE users SET credits=credits+1 WHERE id=?').run(id);db.prepare('INSERT INTO usage(user_id,endpoint,units) VALUES(?,?,1)').run(id,`${ep}:refunded`)})()}catch{}}
async function request(url,opt={},timeout=300000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{return await fetch(url,{...opt,signal:c.signal})}catch(e){if(e?.name==='AbortError')throw Object.assign(new Error('provider_timeout'),{status:504});throw e}finally{clearTimeout(t)}}
async function json(r){const d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(d?.error?.message||d?.error||d?.message||`provider_http_${r.status}`),{status:r.status});return d}
function saveBytes(prefix,ext,bytes){const name=`${prefix}-${Date.now()}-${crypto.randomBytes(6).toString('hex')}.${ext}`;fs.writeFileSync(path.join(mediaDir,name),Buffer.from(bytes));return `/generated-media/${name}`}
async function plan(prompt,options){const r=await generateText({capability:'text',messages:[{role:'system',content:`You are SQ AI's Creative Producer. Convert the user's idea into a compact production plan. Return ONLY valid JSON with keys title, script, scenes, voice. scenes is 1-3 objects with narration, visualPrompt, durationSeconds. voice contains language, dialect, tone, pace. Preserve the user's language and intent. Default Arabic input to Egyptian Arabic. Visual prompts must be directly usable for generation and contain no on-screen text unless requested. Options: ${JSON.stringify(options||{})}`},{role:'user',content:prompt}]});const raw=String(r.text||'').replace(/^```json\s*/i,'').replace(/```$/i,'').trim();try{return JSON.parse(raw)}catch{throw Object.assign(new Error('agent_plan_invalid_json'),{status:502})}}
async function geminiImage(prompt,aspectRatio='1:1'){const key=process.env.GEMINI_API_KEY;if(!key)throw Object.assign(new Error('image_provider_not_configured'),{status:503});const model=process.env.GEMINI_IMAGE_MODEL||'gemini-3.1-flash-image';const r=await request(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseModalities:['IMAGE'],responseFormat:{image:{aspectRatio,imageSize:'1K'}}}})},180000);const d=await json(r);const p=(d?.candidates?.[0]?.content?.parts||[]).find(x=>x?.inlineData?.data||x?.inline_data?.data);const b64=p?.inlineData?.data||p?.inline_data?.data;if(!b64)throw Object.assign(new Error('image_data_missing'),{status:502});const mime=p?.inlineData?.mimeType||p?.inline_data?.mime_type||'image/png';const ext=mime.includes('webp')?'webp':mime.includes('jpeg')?'jpg':'png';return{url:saveBytes('image',ext,Buffer.from(b64,'base64')),provider:'google-gemini',model}}
async function elevenLabsVoice(text,voiceId){const key=process.env.ELEVENLABS_API_KEY;if(!key)throw Object.assign(new Error('voice_provider_not_configured'),{status:503});const id=voiceId||process.env.ELEVENLABS_VOICE_ID;if(!id)throw Object.assign(new Error('voice_id_not_configured'),{status:503});const model=process.env.ELEVENLABS_MODEL||'eleven_multilingual_v2';const r=await request(`https://api.elevenlabs.io/v1/text-to-speech/${encodeURIComponent(id)}`,{method:'POST',headers:{'xi-api-key':key,'Content-Type':'application/json',Accept:'audio/mpeg'},body:JSON.stringify({text,model_id:model,output_format:'mp3_44100_128',voice_settings:{stability:0.45,similarity_boost:0.8,style:0.2,use_speaker_boost:true}})},180000);if(!r.ok)throw Object.assign(new Error((await r.text().catch(()=>'voice_generation_failed')).slice(0,500)),{status:r.status});return{url:saveBytes('voice','mp3',await r.arrayBuffer()),provider:'elevenlabs',model,voiceId:id}}
async function veo(prompt,options={}){const key=process.env.GEMINI_API_KEY;if(!key)throw Object.assign(new Error('video_provider_not_configured'),{status:503});const model=options.model||process.env.VEO_MODEL||'veo-3.1-generate-preview';const body={instances:[{prompt}],parameters:{aspectRatio:options.aspectRatio==='9:16'?'9:16':'16:9',resolution:['720p','1080p','4k'].includes(options.resolution)?options.resolution:'720p'}};const start=await json(await request(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:predictLongRunning`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify(body)},120000));if(!start.name)throw Object.assign(new Error('veo_operation_missing'),{status:502});let state=start;for(let i=0;i<90&&!state.done;i++){await sleep(10000);state=await json(await request(`https://generativelanguage.googleapis.com/v1beta/${state.name}`,{headers:{'x-goog-api-key':key}},60000))}if(!state.done)throw Object.assign(new Error('veo_generation_timeout'),{status:504});if(state.error)throw Object.assign(new Error(state.error.message||'veo_generation_failed'),{status:502});const uri=state?.response?.generateVideoResponse?.generatedSamples?.[0]?.video?.uri;if(!uri)throw Object.assign(new Error('veo_video_uri_missing'),{status:502});const video=await request(uri,{headers:{'x-goog-api-key':key}},180000);if(!video.ok)throw Object.assign(new Error(`veo_download_failed_${video.status}`),{status:502});return{url:saveBytes('video','mp4',await video.arrayBuffer()),provider:'google-veo',model}}
async function mux(videoUrl,audioUrl){if(!videoUrl||!audioUrl)return videoUrl;const root=path.dirname(path.resolve(dbPath));const videoPath=path.join(root,videoUrl.replace(/^\/generated-media\//,'generated-media/').replace(/^\/generated-videos\//,'generated-videos/'));const audioPath=path.join(root,audioUrl.replace(/^\/generated-media\//,'generated-media/'));if(!fs.existsSync(videoPath)||!fs.existsSync(audioPath))return videoUrl;const out=`final-${Date.now()}-${crypto.randomBytes(6).toString('hex')}.mp4`,outPath=path.join(videoDir,out);try{await execFileAsync('ffmpeg',['-y','-i',videoPath,'-i',audioPath,'-map','0:v:0','-map','1:a:0','-c:v','copy','-c:a','aac','-shortest',outPath],{timeout:180000});return`/generated-videos/${out}`}catch(e){console.warn('agent_ffmpeg_mux_failed',e?.message||e);return videoUrl}}

const originalPost=express.application.post;
if(!express.application.__sqaiAgentOrchestrator){express.application.__sqaiAgentOrchestrator=true;express.application.post=function(route,...handlers){if(route==='/api/agent/generate'){const original=handlers[handlers.length-1];handlers[handlers.length-1]=async(req,res,next)=>{const u=currentUser(req);if(!u)return res.status(401).json({error:'authentication_required'});const prompt=clean(req.body?.prompt||req.body?.input,12000);if(!prompt)return res.status(400).json({error:'prompt_required'});const mode=['video','image','voice','all'].includes(req.body?.mode)?req.body.mode:'video';const ep=`/api/agent/generate:${mode}`;if(!charge(u.id,ep))return res.status(402).json({error:'credits_exhausted'});try{const options={aspectRatio:req.body?.aspectRatio==='9:16'?'9:16':'16:9',resolution:['720p','1080p','4k'].includes(req.body?.resolution)?req.body.resolution:'720p'};if(mode==='image'){const d=await geminiImage(prompt,req.body?.aspectRatio==='9:16'?'9:16':'1:1');return res.json({ok:true,mode,result:d,credits_remaining:db.prepare('SELECT credits FROM users WHERE id=?').get(u.id)?.credits??0})}if(mode==='voice'){const d=await elevenLabsVoice(prompt,req.body?.voiceId);return res.json({ok:true,mode,result:d,credits_remaining:db.prepare('SELECT credits FROM users WHERE id=?').get(u.id)?.credits??0})}const p=await plan(prompt,options);const scenes=Array.isArray(p.scenes)&&p.scenes.length?p.scenes.slice(0,3):[{visualPrompt:prompt,narration:prompt,durationSeconds:8}];const outputs=[];if(mode==='all')outputs.push({type:'image',...(await geminiImage(scenes[0].visualPrompt||prompt,options.aspectRatio==='9:16'?'9:16':'1:1'))});const voiceText=clean(scenes.map(s=>s.narration).filter(Boolean).join('\n\n'),12000)||clean(p.script||prompt,12000);let voice=null;if(mode==='all'||req.body?.voice===true){voice=await elevenLabsVoice(voiceText,req.body?.voiceId);outputs.push({type:'voice',...voice})}const videoPrompt=clean(`${scenes.map((s,i)=>`Scene ${i+1}: ${s.visualPrompt||prompt}`).join('\n')}\nNo on-screen text. Cinematic, coherent, realistic motion.`,12000);const video=await veo(videoPrompt,options);let finalVideo=video.url;if(voice)finalVideo=await mux(video.url,voice.url);outputs.push({type:'video',...video,final_url:finalVideo});return res.json({ok:true,mode,plan:p,result:{outputs,video_url:finalVideo,voice_url:voice?.url||null},credits_remaining:db.prepare('SELECT credits FROM users WHERE id=?').get(u.id)?.credits??0})}catch(e){refund(u.id,ep);return res.status(Number(e?.status||502)).json({error:e?.message||'agent_generation_failed',message:'فشل الإنتاج وتم إرجاع الـCredit.'})}}}return originalPost.call(this,route,...handlers)}}
console.log('SQ AI Agent Orchestrator loaded: brief -> plan -> image/voice/video -> MP4');
